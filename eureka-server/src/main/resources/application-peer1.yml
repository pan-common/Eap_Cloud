spring:
  application:
    name: eureka-server
server:
  port: 7001
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      deaultZone: http://localhost:7002/eureka/
